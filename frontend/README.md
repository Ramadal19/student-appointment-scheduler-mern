# Client (Frontend)

This directory contains the frontend code developed in React. Here, we implement the user interface, components, and interaction logic. Run `npm install` and `npm start` to launch the development server.
