# Server (Backend)

This folder holds the backend code using Node.js and Express. It provides API endpoints and handles the business logic. Use `npm install` and `npm run dev` to start the backend server.
