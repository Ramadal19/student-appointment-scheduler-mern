# Dev Container

This folder defines the development container setup (devcontainer.json) used for VS Code or GitHub Codespaces. It ensures a consistent environment with all tools and dependencies preconfigured. Open the project in a dev container for seamless development.
